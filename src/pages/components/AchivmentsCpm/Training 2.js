import React,{useState} from "react";

export const Training = () =>{
/*     const [show,setShow] = useState(false);

    if(!show){ */
        return(
            <section>
                <div class="container">
            
                    <h1 class="title"> Колличество посещенных тренеровок:</h1>
            
                </div>
            </section>
        )
   /*  }else{
        return(
            <section>
                <div class="container">
            
                    <h1 class="title" style={{cursor:'pointer'}} onClick={()=>setShow(false)}>Тренеровка</h1>
                    <div>
                        Training:
                    </div>
                </div>
            </section>
        )
    } */
   
}